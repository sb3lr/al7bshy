# 🛡️ Unified Security Scanner

منصة متكاملة للفحص الأمني الذكي والآمن

<div align="center">

![Cyber Security](https://img.shields.io/badge/Cyber-Security-0f0?style=for-the-badge\&logo=security\&logoColor=white)
![Python](https://img.shields.io/badge/Python-3.13-0f0?style=for-the-badge\&logo=python\&logoColor=white)
![Flask](https://img.shields.io/badge/Flask-2.3-0f0?style=for-the-badge\&logo=flask\&logoColor=white)
![WebSocket](https://img.shields.io/badge/WebSocket-RealTime-0f0?style=for-the-badge\&logo=socket.io\&logoColor=white)

منصة فحص أمني تكشف 4 أنواع رئيسية من الثغرات بواجهة حديثة وتحديث لحظي.

</div>

---

## 📋 المحتويات

* 🎯 نظرة عامة
* ✨ المميزات
* 🔧 التثبيت
* 🚀 التشغيل
* 🎮 استخدام الواجهة
* 🔍 الماسحات المتاحة
* 📊 نتائج الفحص
* 📁 هيكل المشروع
* ⚙️ التقنيات
* ⚠️ ملاحظات أمنية
* 📈 أمثلة
* 🔮 التطوير المستقبلي
* 📄 الرخصة

---

## 🎯 نظرة عامة

**Unified Security Scanner** منصة فحص ويب مبنية بـ **Python + Flask**.
تكتشف الأنواع التالية:

* XSS
* SQL Injection
* IDOR
* CSRF

تستخدم حملات ذكية غير مدمرة، وتعرض النتائج مباشرة عبر WebSocket مع واجهة Cyberpunk.

---

## ✨ المميزات

### 🔐 ماسحات قوية

* **XSS**: أكثر من 10 حملات
* **SQLi**: كشف الخطأ – المنطقي – المؤقت
* **IDOR**: تحليل الـ IDs وتجاوز الوصول
* **CSRF**: تحليل التوكنات والكوكيز

### 🎨 واجهة حديثة

* Cyberpunk style
* عرض تقدم الفحص Live
* تقارير منظمة وخطورة واضحة

### ⚡ أداء

* فحص متزامن
* تأخير ذكي بين الطلبات
* تحليل دقيق + مستويات ثقة

### 🛡️ أمان

* حملات غير مدمرة
* منع الروابط المحلية
* تحذيرات واضحة

---

## 🔧 التثبيت

### المتطلبات

* Python 3.8+
* pip

### الخطوات

```bash
git clone https://github.com/sb3lr/al7bshy.git
cd al7bshy
pip install -r requirements.txt
```

تأكد من التثبيت:

```bash
python --version
pip list | grep Flask
```

---

## 🚀 التشغيل

### تشغيل سريع

```bash
python run.py
```

### تشغيل متقدم

```bash
python app.py --port 8080
python app.py --host 0.0.0.0
python app.py --debug false
```

عند النجاح يظهر:

```
🛡️   UNIFIED SECURITY SCANNER PLATFORM v2.0
Web Interface: http://127.0.0.1:5000
API Base URL: http://127.0.0.1:5000/api
WebSocket: ws://127.0.0.1:5000
```

---

## 🎮 استخدام الواجهة

1. افتح:

```
http://localhost:5000
```

2. أدخل الرابط المراد فحصه
3. اختر وضع الفحص:

   * 🟢 Normal
   * 🟡 Passive
   * 🔴 Aggressive
4. اضغط START وراقب النتائج Live.

---

## 🔍 الماسحات المتاحة

### 1. XSS

* Reflected / Stored / DOM
* كشف onerror/onload/scripts
* 10+ بايلود ذكي

### 2. SQL Injection

* Boolean – Error – Time
* MySQL / PostgreSQL / MSSQL / Oracle

### 3. IDOR

* التلاعب بالـ IDs
* اكتشاف الوصول غير المصرح

### 4. CSRF

* تحليل توكن
* SameSite / HttpOnly / Secure

---

## 📊 نتائج الفحص

### مستويات الخطورة

* 🔴 **CRITICAL** (>0.8)
* 🟡 **HIGH** (>0.7)
* 🟠 **MEDIUM** (>0.5)
* 🟢 **LOW** (<0.5)

### مثال

```json
{
  "type": "SQL Injection",
  "confidence": 0.9,
  "risk": "high",
  "issue": "SQL error detected",
  "location": "search.php?test=query",
  "recommendation": "Use parameterized queries"
}
```

---

## 📁 هيكل المشروع

```
# 📁 **شرح تفصيلي لهيكل المشروع**

## 🏗️ **الهيكل العام**

security-scanner/
│
├── 📄 app.py                    # 🎯 الدماغ - التطبيق الرئيسي
├── 📄 run.py                    # 🚀 المحرك - ملف التشغيل
├── 📄 requirements.txt          # 📦 الأدوات - المتطلبات
├── 📄 README.md                 # 📚 الدليل - الوثائق
│
├── 📁 core/                     # 🧠 النظام العصبي - المكونات الأساسية
│   ├── 📄 config.py            # ⚙️ الإعدادات - التكوين
│   └── 📄 scanner_base.py      # 🧬 الجين الأساسي - فئة الماسح الأساسية
│
├── 📁 scanners/                 # 🔍 الأسلحة - الماسحات الأمنية
│   ├── 📄 xss_scanner.py       # 🎯 XSS - مكافحة البرمجة النصية
│   ├── 📄 sqli_scanner.py      # 💉 SQLi - مكافحة حقن SQL
│   ├── 📄 idor_scanner.py      # 🔓 IDOR - مكافحة الوصول غير المصرح
│   └── 📄 csrf_scanner.py      # 🎣 CSRF - مكافحة التزوير
│
└── 📁 templates/                # 🎨 الواجهة - الجماليات
    └── 📄 index.html           # 🖥️ الشاشة - الواجهة الرئيسية
```

---

## 🔍 **تفصيل كل ملف ودوره**
```
### **1. 🎯 `app.py` - الدماغ الرئيسي**
```python
# 🏛️ الوظيفة: التطبيق الرئيسي الذي يدير كل شيء
# 🔗 العلاقات: يتصل بـ ALL الملفات

├── يدير خادم Flask          ← يستقبل الطلبات من المستخدم
├── يدير WebSocket           ← يوصل النتائج مباشرة للمتصفح
├── يستدعي الماسحات الأربعة  ← ينظم عملية الفحص
├── يجمع النتائج             ← يوحد نتائج الماسحات
├── يولد التقارير            ← يصنع الملخص النهائي
└── يتصل بقاعدة البيانات     ← يخزن النتائج (ذاكرة مؤقتة)
```

**🔗 علاقات:** 
```
- ← `run.py` (يستدعي)
- → `core/config.py` (يقرأ الإعدادات)
- → `scanners/*.py` (ينادي الماسحات)
- → `templates/index.html` (يرسل HTML)
```

---

### **2. 🚀 `run.py` - المحرك**
```python
# ⚡ الوظيفة: نقطة البداية - يبدأ تشغيل التطبيق
# 🔌 العلاقات: المدخل الرئيسي للمستخدم

├── يفحص صحة النظام         ← يتأكد من وجود الملفات
├── يعالج الأخطاء           ← يصيد المشاكل قبل البدء
├── يطبع رسائل الترحيب      ← يعطي التعليمات
└── يشغل الخادم             ← يبدأ app.py
```

**🔗 علاقات:**
- → `app.py` (يشغل)
- ← المستخدم (يستدعي)

---

### **3. 📦 `requirements.txt` - الأدوات**
```txt
# 🛠️ الوظيفة: قائمة جميع الأدوات اللازمة
# 📚 العلاقات: بيئة العمل

Flask==2.3.0            ← إطار الويب
Flask-SocketIO==5.3.4   ← الاتصال المباشر
requests==2.31.0        ← طلبات الإنترنت
beautifulsoup4==4.12.0  ← تحليل HTML
```

**🔗 علاقات:**
- ← `pip` (يقرأ)
- → جميع ملفات Python (توفر الأدوات)

---

### **4. 📚 `README.md` - الدليل**
```markdown
# 📖 الوظيفة: وثائق المشروع والتعليمات
# 🗺️ العلاقات: خريطة المستخدم

├── يشرح الفكرة             ← لماذا المشروع؟
├── يعلم كيفية التركيب       ← كيف أبدأ؟
├── يعلم كيفية التشغيل       ← كيف أشغله؟
├── يعطي أمثلة              ← كيف أستخدمه؟
└── يشرح البنية             ← كيف يعمل؟
```

**🔗 علاقات:**
- ← المستخدم (يقرأ)
- → جميع الملفات (يشرحها)

---

## 🧠 **مجلد `core/` - النظام العصبي**

### **5. ⚙️ `core/config.py` - الإعدادات**
```python
# 🎛️ الوظيفة: يخزن إعدادات النظام
# ⚙️ العلاقات: اليد اليمنى لـ app.py

class PlatformConfig:
    ├── host = "127.0.0.1"      ← عنوان الخادم
    ├── port = 5000             ← المنفذ
    ├── secret_key = "..."      ← المفتاح السري
    ├── debug = True            ← وضع التصحيح
    └── timeout = 30            ← وقت الانتظار
```

**🔗 علاقات:**
- ← `app.py` (يستدعي)
- ← جميع الماسحات (تقرأ الإعدادات)

---

### **6. 🧬 `core/scanner_base.py` - الجين الأساسي**
```python
# 🧬 الوظيفة: الفئة الأم لجميع الماسحات
# 🧬 العلاقات: الأب لجميع الماسحات

class BaseScanner:
    ├── initialize()       ← يهيئ الاتصال بالإنترنت
    ├── fetch_page()       ← يجلب صفحة ويب
    ├── post_form()        ← يرسل نموذج
    ├── add_vulnerability() ← يسجل ثغرة
    ├── delay()            ← ينتظر بين الطلبات
    └── get_results()      ← يعطي النتائج
```

**🔗 علاقات:**
- ← جميع الماسحات الأربعة (ترث منه)
- → `app.py` (توفر البنية الأساسية)

---

## 🔍 **مجلد `scanners/` - الأسلحة**

### **7. 🎯 `scanners/xss_scanner.py` - سلاح XSS**
```python
# 🎯 الوظيفة: يكتشف ثغرات XSS
# 🎯 العلاقات: الطفل الأول لـ BaseScanner

class XSSScanner(BaseScanner):
    ├── _generate_payloads() ← يصنع حمولات XSS ذكية
    ├── scan()              ← يبدأ الفحص
    ├── _test_form()        ← يختبر النماذج
    ├── _test_url_parameters() ← يختبر بارامترات URL
    └── _analyze_response() ← يحلل الاستجابة
```

**🔗 علاقات:**
- → `BaseScanner` (يرث منه)
- ← `app.py` (يستدعيه)

**💡 مثال عمل:**
```
1. يدخل: <script>alert(1)</script>
2. يرسلها لموقع الاختبار
3. يفحص إذا ظهرت في الرد
4. يسجل النتيجة
```

---

### **8. 💉 `scanners/sqli_scanner.py` - سلاح SQL Injection**
```python
# 💉 الوظيفة: يكتشف ثغرات حقن SQL
# 💉 العلاقات: الطفل الثاني لـ BaseScanner

class SQLiScanner(BaseScanner):
    ├── payloads = ["'", "OR 1=1", ...] ← قائمة الحمولات
    ├── db_error_patterns = {...}      ← أنماط أخطاء SQL
    ├── scan()                         ← يبدأ الفحص
    ├── _test_form()                   ← يختبر النماذج
    └── _analyze_response()            ← يبحث عن أخطاء SQL
```

**🔗 علاقات:**
- → `BaseScanner` (يرث منه)
- ← `app.py` (يستدعيه)

**💡 مثال عمل:**
```
1. يدخل: ' OR '1'='1
2. يرسلها لنموذج تسجيل دخول
3. يبحث عن أخطاء SQL في الرد
4. يسجل النتيجة
```

---

### **9. 🔓 `scanners/idor_scanner.py` - سلاح IDOR**
```python
# 🔓 الوظيفة: يكتشف ثغرات الوصول غير المصرح
# 🔓 العلاقات: الطفل الثالث لـ BaseScanner

class IDORScanner(BaseScanner):
    ├── _scan_for_idor_patterns() ← يبحث عن أنماط معرفات
    ├── _test_common_idor_patterns() ← يختبر أنماط شائعة
    ├── _test_idor_access()       ← يختبر الوصول
    └── _is_error_page()          ← يتحقق من صفحات الخطأ
```

**🔗 علاقات:**
- → `BaseScanner` (يرث منه)
- ← `app.py` (يستدعيه)

**💡 مثال عمل:**
```
1. يجد رابط: /user/123
2. يحاول: /user/124
3. يفحص إذا استطاع الوصول
4. يسجل النتيجة
```

---

### **10. 🎣 `scanners/csrf_scanner.py` - سلاح CSRF**
```python
# 🎣 الوظيفة: يكتشف ثغرات التزوير
# 🎣 العلاقات: الطفل الرابع لـ BaseScanner

class CSRFScanner(BaseScanner):
    ├── _analyze_form_csrf()      ← يحلل النماذج
    ├── _check_cookies_security() ← يفحص الكوكيز
    ├── _check_cors_headers()     ← يفحص رؤوس CORS
    └── _is_critical_form()       ← يحدد أهمية النموذج
```

**🔗 علاقات:**
- → `BaseScanner` (يرث منه)
- ← `app.py` (يستدعيه)

**💡 مثال عمل:**
```
1. يفحص النماذج في الصفحة
2. يبحث عن tokens CSRF
3. يفحص أمان الكوكيز
4. يسجل النتيجة
```

---

## 🎨 **مجلد `templates/` - الواجهة**

### **11. 🖥️ `templates/index.html` - الشاشة**
```html
<!-- 🎨 الوظيفة: الواجهة التي يراها المستخدم -->
<!-- 🖥️ العلاقات: الوجه الذي يظهر للعالم -->

├── تصميم Cyberpunk            ← أنماط نيون خضراء
├── JavaScript مع WebSocket    ← تحديث مباشر
├── أدوات التحكم               ← أزرار وإدخالات
├── عرض النتائج               ← لوحة النتائج
└── إحصائيات الفحص            ← ملخص المعلومات
```

**🔗 علاقات:**
- ← `app.py` (يرسلها للمتصفح)
- → المستخدم (يراها)
- → JavaScript (يتصل بـ WebSocket)

---

## 🔄 **كيف يتصلون مع بعض؟**

### **سير العمل الكامل:**
```
┌─────────────┐
│   المستخدم   │
└──────┬──────┘
       ↓ (يضغط زر)
┌─────────────┐
│   index.html │
└──────┬──────┘
       ↓ (يرسل WebSocket)
┌─────────────┐
│    app.py    │ ← يأخذ الطلب
└──────┬──────┘
       ↓ (ينادي الماسحات)
┌─────────────────┐
│   جميع الماسحات  │ ← تعمل بالتسلسل
└────────┬────────┘
       ↓ (تعيد النتائج)
┌─────────────┐
│    app.py    │ ← يجمع النتائج
└──────┬──────┘
       ↓ (يرسل عبر WebSocket)
┌─────────────┐
│   index.html │ ← تعرض النتائج
└──────┬──────┘
       ↓
┌─────────────┐
│   المستخدم   │ ← يرى النتيجة
└─────────────┘
```

### **تسلسل الأحداث:**
1. **المستخدم** ← يدخل موقعاً في `index.html`
2. **index.html** ← يرسل لـ `app.py` عبر WebSocket
3. **app.py** ← يستدعي `config.py` للإعدادات
4. **app.py** ← ينادي الماسحات الأربعة واحداً تلو الآخر
5. **كل ماسح** ← يرث من `scanner_base.py`
6. **كل ماسح** ← يجلب الصفحة ويفحصها
7. **كل ماسح** ← يعيد النتائج لـ `app.py`
8. **app.py** ← يجمع النتائج ويصنع تقريراً
9. **app.py** ← يرسل النتائج لـ `index.html`
10. **index.html** ← تعرض النتائج للمستخدم

---

## 🎭 **تشبيه المسرح:**

```
🎭 مسرح الأمن السيبراني:

🏛️  المسرح (app.py) - يدير العرض
🎭  الممثلون (الماسحات) - يؤدون الأدوار
📜  النص (scanner_base.py) - يحدد الحركات
🎪  الديكور (index.html) - يجذب الجمهور
🎟️  التذاكر (run.py) - يدخل الجمهور
📖  البرنامج (README.md) - يشرح المسرحية
🎛️  الإضاءة (config.py) - يضبط الأجواء
```

**كل ملف له دور محدد، وكلهم يعملون معاً ليعطوا عرضاً رائعاً!** 🎉```

---

## ⚙️ التقنيات

### Backend

* Flask
* Flask-SocketIO
* Requests
* BeautifulSoup4
* Flask-CORS

### Frontend

* HTML5, CSS3 Cyberpunk
* JS ES6 + WebSocket
* Socket.IO client

### Security

* Safe payloads
* Intelligent delays
* Exception handling

---

## ⚠️ ملاحظات أمنية

### ممنوع:

* فحص مواقع بدون إذن
* أي استخدام تخريبي
* استهداف أنظمة حقيقية بدون تصريح

### مسموح:

* مواقع تدريب
* مواقعك
* التعليم والبحث

### مواقع تدريب آمنة:

```
http://testphp.vulnweb.com
https://juice-shop.herokuapp.com
http://zero.webappsecurity.com
```

---

## 📈 أمثلة

### موقع: testphp.vulnweb.com

```
XSS: 6
SQLi: 5
IDOR: 1
CSRF: 2
الخطورة: HIGH
```

### موقع: juice-shop

```
XSS: 0
SQLi: 0
IDOR: 0
CSRF: 1 (تحذير كوكيز)
الخطورة: LOW
```

---

## 🔮 التطوير القادم

* ماسح RFI/LFI
* ماسح XXE
* ماسح SSRF
* تقارير PDF
* واجهة إدارة
* API كاملة

## 📄 الرخصة

MIT License
للاستخدام التعليمي والبحثي فقط.
المطور غير مسؤول عن أي إساءة استخدام.

---

<div align="center">
⭐ إذا أعجبك المشروع لا تنسَ النجمة  
"الأمن مسؤولية جماعية"
</div>
